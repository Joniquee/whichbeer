// Local-dev default. In production this file is regenerated at deploy time
// from the API_BASE environment variable (see frontend/README or deploy
// script), so index.html always reads window.API_BASE instead of a
// hardcoded URL.
window.API_BASE = "http://localhost:8000";
